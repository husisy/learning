import os
import gzip
import shutil

# TODO
# hf_file = lambda *x: os.path.join('tbd00', *x)
# if not os.path.exists(hf_file()):
#     os.mkdirs(hf_file())

# with gzip.open('test.txt.gz', 'wb') as file1:
#     file1.write(b"test text")
# with gzip.open('test.txt.gz', 'rb') as file1:
#     print(file1.read())

# with open('test.txt','wb') as file1:
#     file1.write(b'test text')

# with open('test.txt', 'rb') as file1:
#     with gzip.open('test.txt.gz', 'wb') as file2:
#         shutil.copyfileobj(file1, file2)

# with gzip.open('test.txt.gz', 'rb') as file1:
#     with open('test.txt', 'wb') as file2:
#         shutil.copyfileobj(file1, file2)
